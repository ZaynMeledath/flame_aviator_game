import 'package:flame/game.dart';

class Config {
  static const gameHeight = 400.0;
  static final planeSize = Vector2(80, 50);
  static final takeOffAcceleration = Vector2(150, -230);
  static const parallaxDotsWidth = 35.0;
  static const parallaxDotsVelocity = 45.0;
}
